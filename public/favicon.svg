<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Left vertical stem (black) -->
  <rect x="6" y="5" width="5" height="22" fill="#0a0a0a"/>
  <!-- Top horizontal bar (black) -->
  <rect x="6" y="5" width="20" height="5" fill="#0a0a0a"/>
  <!-- Middle horizontal bar (black) -->
  <rect x="6" y="13.5" width="15" height="5" fill="#0a0a0a"/>
  <!-- Bottom horizontal bar (green) -->
  <rect x="6" y="22" width="20" height="5" fill="#05c900"/>
</svg>
